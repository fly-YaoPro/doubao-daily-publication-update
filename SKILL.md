---
name: doubao-daily-publication-update
description: 每日核对全部机构执行文档中的发布，除处理当天记录外，还逐日回溯本周周一至当天，核对抖音与小红书客户发布表的漏登、跨日冲突和机构归属色块；公开互动数据只抓抖音，小红书禁止自动打开作品页或抓取互动。用于“每日发布表更新”“更新今日发布”“核对本周发布”“同步发布表”“补录发布表”“更新互动数据”等任务。
---

# 每日发布表更新

## 目标

把机构当天真实发布的工作任务内容准确补进对应客户发布表，并留下可复核结果。这个 Skill 处理两平台发布登记，但发布后公开互动只处理抖音；小红书不再自动访问作品页。它不替代产能盘点、水评判定或脚本审核。

## 强制流程

1. 先完整读取 [references/source-map.md](references/source-map.md) 与 [references/field-and-match-policy.md](references/field-and-match-policy.md)。
2. 执行 `scripts/run.mjs` 做全机构只读盘点。不得只抽查闪光点，也不得拿机构多维表或全量表代替每日执行文档。
3. 发布来源必须是：本周总执行文档 → “二、机构执行表格” → 每家当周执行文档 → 对应日期 → “5.3 机构发布视频”；自建达人例外，直接读取本周总执行文档“每日执行看版”中当天的“日期-自建达人”区块及其“5.3 机构发布视频”。每次运行先处理当天，再按同一口径逐日回溯本周周一至当天，确保晚补到执行文档的历史发布能够补进客户表。执行文档可能包含同步块和嵌套日期标题；每张“5.3”发布表必须归属于它前面最近的日期标题，遇到新的日期标题立即切换。发布日期单元格为空时只能继承这个最近日期，禁止默认成任务运行日或外层章节日期。
4. `内容方向` 为 APP、豆包 APP、手账 Skill、手账贴纸 Skill、专家模式或明显 APP 端错别字的记录排除；其中“手账 Skill / 手账贴纸 Skill”已经确认属于 APP 端，“专家模式”不属于工作任务客户发布表，二者均不得登记进工作任务客户表、不得计入 PC 产能。不确定的记录进入人工复核，禁止自行归类。
5. 当日补录和本周回溯只使用机构当周执行文档及其中明确链接的主页、稿件和发布证据。机构次日上午会把发布状态补进自己的多维执行表，属于 `$doubao-institution-publication-sync-audit` 的考核来源；本 Skill 不读取、不修改机构多维表或全量表。同名达人跨机构、跨项目、跨时间不得串联。
6. 去重优先级固定为：GID/作品 ID → 规范化发布链接 → 达人昵称 + 发布日期。抖音主页弹层链接中的 `modal_id/vid` 也视为作品 ID。最后一种只允许唯一命中；多条命中必须停下复核。同一稳定作品 ID 已在更早日期登记时，按历史重复自动排除，不再制造待人工核对项，也不得继续进入当天机构色块、互动抓取、审核状态同步或任何指标链路；只有缺少稳定作品 ID、仅链接证据跨日冲突时才进入待核。
7. 只补客户表中明确缺失的记录。每次先读取两张客户发布 Sheet 的实时表头，再按字段语义定位目标列，禁止把抖音和小红书套成同一列结构：抖音 E 列是“发布平台”，小红书 E 列是“内容形式”。写入前确认当前 revision 和目标空行；格式模板必须取最近一个更早发布日期的可信历史行，禁止把同一批刚新增的上一行继续当模板，防止格式错误连锁复制。再按实时表头显式复制所有胶囊字段的 `data_validation`，并把新增行固定为客户表标准 27px 行高；写入后回读整行并验证 URL、下拉字段、选中值、行号与行高。
8. 在客户表最后一列写机构归属：抖音 `AR`，小红书 `Z`。12 家外部机构写既定机构色块；执行文档中的“自建”达人归属机构“自建”，末列写文字“自建”而不是借用其他机构颜色。已有记录也要补齐正确归属标记。
   每次本周回溯都以“当前机构执行文档”作为机构归属，重放已匹配客户记录的末列色块；颜色错填只修客户表末列，不改机构执行文档。执行文档始终由机构维护，豆尼只读。
9. 发布登记后必须同步客户审核状态：抖音只更新【工作任务-抖音视频审核】，小红书只更新【工作任务-小红书视频审核】。实时按表头定位“目前进度”，不得写死列号；作品已确认发布且审核记录唯一匹配后，把状态改为“已发布”，并保留下拉验证和胶囊样式。已是“已发布”则幂等跳过；即使发布表此前已登记，下次任务仍要补齐漏改的审核状态。
10. 审核记录按“执行文档内容方向 + 当前合作优先”匹配。执行文档“5.3 机构发布视频”的`内容方向`是专项强证据：先区分工作任务/办公任务与课程教学（兼容旧称科普、科普号、科普专项）；若写明“课程教学第一期/第二期”等期数，必须保留并与客户审核表稿件标题中的专项和期数交叉验证，禁止把工作任务发布写到课程教学审核行，也禁止把课程教学写到错误期次。然后锁定同平台、同专项和同达人；达人匹配忽略大小写、空格、尾部机构备注和 emoji。再排除已经发布、暂停或取消的历史合作；若只剩一条当前记录，仍须确认稿件标题没有明确专项冲突后才能命中。多条时必须读取客户审核表稿件单元格中的真实链接，并用 `drive +inspect` 解析 canonical URL 与文档标题，不能因为表格显示文本没有标题或来源侧没带稿件字段就提前报人工；随后结合主页和发布日期所在周期继续消歧，证据仍相同时，只允许在唯一最新周期时回退。客户审核表周期未及时滚动不影响唯一当前记录；内容方向与稿件标题冲突、多条当前合作同分、或多条发布指向同一审核行时不得猜，进入人工复核。发布文案或标题相同绝不等于同一达人、同一次合作：达人身份不一致时，禁止借用另一行的主页、稿件、审核状态或其它字段；发布作品证据已确认时可以登记真实作品，但无法唯一匹配的关联字段必须留空并单独待核。
11. 发布登记后只对当天抖音有效发布调用公开作品详情抓取器，写入“赞、评、藏、转发、互动总和”并逐行回读。互动总和按赞 + 评 + 藏 + 转发计算；无法稳定取得的播放量留空，不估算。小红书只做发布补录、机构色块和审核状态同步：禁止打开小红书作品页、跟随 `xhslink.cn` 跳转、调用小红书抓取器或回填小红书互动数据；已有互动字段保留原值，不清空、不覆盖，也不把“未抓取”列为待核异常。
12. 试运行结果只私聊遥师。正式内部群版本必须 @一朵（刘朵）人工复核；不得发送客户群或供应商群。
13. 主页、发布链接和稿件链接统一以**普通单元格 value** 写入原始 URL，让飞书自动生成显示标题；技术实现禁止传 `rich_text`、`linkUrl` 或任何自定义显示文本，也不得写“查看稿件”等标题超链接。只有来源里本来就存在、并已验证能打开的短链接才允许使用；禁止根据作品 ID 猜测 `/explore/{id}` 等路径。无法确认短链时保留执行文档中的完整原始 URL，并保持标准行高。稿件为飞书文档时，客户审核 Sheet 的稿件单元格为权威来源：`mention_type=22` 按 docx token 解析，`mention_type=16` 按 Wiki node 解析，随后必须用 `drive +inspect` 得到真实资源类型和 canonical URL；任何执行文档里残留的猜测地址都不能覆盖审核 Sheet。解析或点击验证失败时该条进入待核，禁止把看似完整但打不开的 URL 写入客户表。写后除回读单元格外，还必须重新验证点击目标可解析，不能把“接口能读到缓存正文”当成链接可用。
14. 单条记录缺发布链接、字段异常时，只把该条放入待核，并继续处理其余记录；禁止因一条脏数据中止整批。抖音公开互动抓取若整体登录态失效或触发平台风控，只生成一条平台级系统异常，不得为每个达人复制同一错误。小红书不存在抓取失败重试链路。达人昵称匹配允许忽略大小写、空格、emoji 和尾部“（公司/机构备注）”，但同名跨机构、跨日期仍必须以机构和作品证据隔离。机构把平台胶囊填错时，以发布链接域名（抖音/小红书）纠正写入路由，并在执行日志保留原值；不得因胶囊错填把作品写进另一平台客户表。
15. 每次运行必须把“本周总执行文档”中的来源与已配置的 13 家活跃机构清单（含自建）做覆盖核对：12 家外部机构核对当周执行文档入口，自建核对“每日执行看版”里的当天“自建达人”区块。缺少来源时明确报告；不得因其当日没有解析出记录而误报为零发布。
16. 分块读取 Sheet 时必须尊重飞书返回的实时行数：预设上限超过当前表尾时，截到真实末行或正常结束，不得把 `range entirely outside sheet bounds` 当成整单失败。飞书网络、transport、EOF、429、502/503/504 等瞬时错误按短退避自动重试；字段错误、权限不足和无法解析等确定性错误不得重试掩盖。某一家机构在重试后仍不可用时，标记该来源待核并继续其他机构；客户目标表不可读时必须停止写入。

## 运行模式

- **每日更新**：由“每日发布表更新、更新今日发布、核对本周发布、同步发布表、补录发布表”等词触发。每次读取当周执行文档，处理当天并回溯周一至当天；历史缺失补进客户表，已存在记录复核机构色，跨日冲突进入待核。
- **历史互动复查**：由“更新互动数据、补齐某日互动数据”等明确词触发。仅允许复查抖音；仍以执行文档和客户发布表中的作品 ID/发布链接为准。小红书请求只说明自动抓取已停用，不打开页面、不启动登录或扫码、不写互动字段。

群聊触发时必须 @豆尼；仅允许已登记的内部群和内部同事调用。两个模式都走确定性流程，不进入内容创作 Skill 的确认链。

## 执行命令

只读盘点：

```powershell
node scripts/run.mjs --compact
```

指定日期：

```powershell
node scripts/run.mjs --date 2026-08-17 --compact
```

仅在已明确授权后补末列机构色：

```powershell
node scripts/run.mjs --date 2026-08-17 --compact --apply-markers
```

脚本输出中的 `missing` 是待补录候选，不等于允许直接写入。必须先完成来源补全、去重和 revision 复核。

## 输出要求

结论必须使用飞书 Card 2.0，不得把 Markdown 字符串或转义后的 `\\n` 当普通文本发送。卡片至少包含：

- 日期、扫描机构数、当日有效发布数；
- 当周回溯起止日期、已核天数、历史发布数、历史补登数、历史待核数和机构归属标记核对行数；
- 机构分布：每家展示总数，并在同一项内列出抖音数和小红书数；不得再单独拆一块平台分布；
- 互动抓取固定分平台显示：`抖音已完成/需复核/无当日发布；小红书已停用`。不得在卡片中展示抓取条数；抖音赞、评、藏、转发及互动总和只写客户表并保留执行日志；
- APP 排除项、冲突和失败来源；
- 两张工作任务审核 Sheet 的状态同步结果只写“已完成 / 需复核 / 无当日发布”；
- 两张客户表和本周总执行文档的可点击链接；
- 正式群版本中的 `<at id=ou_1948f811b81d776b153d038893751bc3></at>`。

推荐版式：蓝色标题栏；首屏指标卡展示当日有效发布和本次补录；机构分布与检查结果并列，机构项写成“机构 总数（抖音 n / 小红书 n）”；检查结果只写 APP 排除、互动抓取状态、待核和复查后仍缺；底部使用两个同级、同宽、`default` 样式的 `open_url` 按钮并列跳转两张客户发布表，不设置主次色。

不得输出内部 `source_xxx`、原始日志或无法点击的技术 ID。

## 运行约束

- 本 Skill 不定义执行时间；定时启停和时间调整由 Daemon 调度台管理。
- 无论人工触发还是调度触发都必须幂等：同一发布记录不得重复新增；同一日期同一结论不得重复发送。
- 服务重启后复用持久化检查点，不以旧快照代替当轮飞书数据。
